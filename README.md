# Python Template

This repository contains templates for a python project. Clone and run `chmod +x setup.sh && ./setup.sh` to set up your project.

## How to update this template

```console
git remote set-url origin "https://$GIT_PERSONAL_TOKEN@github.com/20tyamato/python-template.git"
git push -u origin main
```
