# Python Template

This repository provides a template for Python projects.
Use the steps below to quickly set up a new project.

## 🚀 Setup

```bash
git clone https://github.com/20tyamato/python-template.git project-name
cd project-name
chmod +x setup.sh
./setup.sh
```

⚠️ Note: Before running the setup script, make sure you have created a new empty repository on your GitHub account for your project.

## 🔄 How to Update This Template

If you want to update the template itself and push changes to the main branch:

```bash
git remote set-url origin "https://$GIT_PERSONAL_TOKEN@github.com/20tyamato/python-template.git"
git push -u origin main
```
